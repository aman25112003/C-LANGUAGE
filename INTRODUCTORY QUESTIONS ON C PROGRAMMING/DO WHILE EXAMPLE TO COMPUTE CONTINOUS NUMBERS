#include <stdio.h>
#include <conio.h>
int main()
{
    int a, index = 0;
    printf("ENTER A NUMBER");
    scanf("%d", &a);
    do
    {
        printf("%d\n", index);
        index = index + 1;
    } while (index < a);
    return 0;
}
