#include <stdio.h>
#include <conio.h>
int main()
{
    int n,flag=0;
    printf("ENTER THE INTEGER");
    scanf("%d",&n);
    while(n!=0)
    {
        n=n/10;
        flag=flag+1;
    }
    printf("THE NUMBER OF DIGITS IN THE INTEGER IS %d",flag);
    return 0;
}
