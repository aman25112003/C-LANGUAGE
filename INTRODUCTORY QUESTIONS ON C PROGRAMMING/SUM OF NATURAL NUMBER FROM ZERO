#include <stdio.h>
#include <conio.h>
int main()
{
    int n,i,sum=0;
    printf("ENTER THE INTEGER UPTO WHICH SUM IS NEEDED ");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("%d",sum);
    return 0;
}
