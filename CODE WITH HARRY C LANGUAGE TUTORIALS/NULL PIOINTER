//NULL POINTER IS A POINTER WHICH HAS A VALUE RESERVED FOR INDICATING THAT THE POINTER OR REFERENCE DOES NOT REFER TO A VALID OBJECT
//A NULL POINTER IS GUARANTEED TO COMPARE UNEQUAL TO ANY POINTER THAT POINTS TO A VALID OBJECT
//DEREFERENCING A NULL POINTER IS UNDEFINED BEHAVIOR IN C , AND A CONFORMING IMPLEMENTATION IS ALLOWED TO ASSUME THAT ANY POINTER THAT IS DEREFERENCED IS NOT NULL
//NULL POINTER IS A SPECIFIC POINTER
//NULL POINTER GIVES A FUNCTIONALITY TO C PROGRAMMER TO CHECK WHETHER A POINTER IS LEGITIMATE OR NOT

#include <stdio.h>
int main()
{
    int a=34;
    int *ptr=&a;
    if(ptr!=NULL)
    {
        printf("THE ADRESS OF a IS %d\n",*ptr);
    }
    else
    {
        printf("THE PINTER IS NULL POINTER AND CANNOT BE DEREFERENCED\n");
    }
    
    return 0;
}
